Arquivos referentes ao processo de seleção para o projeto PBAD/LabSEC e outros
projetos que trabalham com a linguagem de programação Java. A base de código
está em formato Maven, e portanto, a execução de

    mvn package

nesta pasta cria um pacote para distribuir o código, que estará em

    target/hiring-0.2.3-src.tar.gz.

Para gerar artefatos rapidamente em uma base de código completa, execute

    mvn compile exec:java.

Para compilar o documento em LaTeX, o pacote `tikz` [1] é necessário.

[1] https://www.ctan.org/pkg/pgf
